# Pre-launch аудит — ds-analytics

Дата аудита: 2026-08-23 · Обновлён: 2026-08-23 (закрыт блок C)

Состояние на момент аудита: кода не было, только паспорт проекта. Блок C с тех пор выполнен — см. ниже.

---

## Вердикт

Паспорт проекта сильный: позиционирование отстроено от конкурентов, ограничения производительности осознаны, DoD для правила написан. Но **между паспортом и запуском лежат пять групп работ, три из которых — блокеры**, и ни одна не является «написать больше правил».

Главный риск проекта — **не производительность и не количество правил, а два обещания, которые дал паспорт, но которые Figma Plugin API не отдаёт даром**: атомарный undo массового фикса и инкрементальный ре-скан по хэшу ноды. Оба заявлены как решённые (`fixes/applier.ts` — «транзакции», `scanner/cache.ts` — «хэш по version-подобной сигнатуре»), но ни у того ни у другого нет прямой опоры в API. См. блок B — это надо закрыть проектным решением до первой строки `main/`.

Порядок работ: **A → B → C → E → D**. Блок A выполняется до кода и может отменить проект.

**Статус на 2026-08-23:** снято **5 блокеров из 7**. Закрыты B1–B4 (стратегия undo, инвалидация кэша, обход при dynamic-page, библиотечные переменные), блок C целиком и лицензия. Все четыре механизма, которые паспорт декларировал без опоры в API, теперь имеют принятые решения, реализацию и тесты; решения лежат в [docs/decisions/](decisions/).

**Осталось два блокера, и оба упираются не в код.** Политика приватности написана черновиком и ждёт вашей проверки. Блок A — главный: нужны три боевых файла и дизайнер, который будет судить срабатывания. Процедура для него готова, данных нет.

Замечание о методе: решения B2–B4 приняты после сверки с `@figma/plugin-typings` из `package-lock.json`. Одна из рекомендаций самого аудита (B2, `documentchange`) при этой сверке оказалась неверной и заменена — поправка отмечена по месту.

---

## Блок A. Валидация сигнала — гейт №0

Паспорт сам ставит этот гейт («если сигнал ниже 70% — продукт мёртв»), но не описывает процедуру. Без процедуры гейт не сработает: будет соблазн засчитать его «на глаз».

**Процедура написана 2026-08-23: [docs/validation/protocol.md](validation/protocol.md), таблица для заполнения — [results.md](validation/results.md).** Ниже — конспект; детали (выборка, рубрика вердиктов, пороги решений) в протоколе.

### A1. Зафиксировать методику замера

- [ ] Взять **3 боевых файла разного профиля**: зрелая ДС с библиотекой, продуктовый файл-потребитель библиотеки, файл-помойка без ДС. Один файл даст нерепрезентативную цифру.
- [ ] Прогнать вручную 5 правил-кандидатов. Обязательно включить **`components/detached-instance`** — это самое эвристичное правило в каталоге и вероятный источник провала по сигналу.
- [ ] Для каждого срабатывания зафиксировать вердикт живого дизайнера: `реальная проблема` / `ложное` / `намеренно так`. Третья категория — не ложное срабатывание, но и не проблема; она определяет объём работы по ignore-листу.
- [ ] Сигнал = `реальные / (реальные + ложные)`. Считать **по каждому правилу отдельно**, не средним по больнице: одно шумное правило утопит доверие ко всем остальным.

### A2. Отдельно провалидировать дифференциатор

`tokens/layer-violation` — правило, ради которого проект существует. Оно требует поля `tokenLayers` в конфиге.

- [ ] Проверить на боевых файлах, что **коллекции переменных вообще именуются так, что слои различимы**. Если в реальных ДС коллекции называются `Colors`, `Colors 2`, `New colors final` — правило неприменимо без ручной настройки на каждом файле, и его ценность как «фичи из коробки» падает до нуля.
- [ ] Если различимость низкая — заложить **мастер настройки слоёв при первом запуске** (показать список коллекций, попросить разметить). Это меняет объём MVP и должно быть решено сейчас, а не после релиза.

### A3. Критерий выхода из блока

Ни строчки кода правил, пока нет таблицы «правило → сигнал → вердикт (берём / в `info` / выкидываем)» по 3 файлам.

---

## Блок B. Архитектурные дыры — закрыть до кода ⚠️ БЛОКЕР

Это находки аудита: места, где паспорт декларирует результат, но не декларирует механизм, а механизм неочевиден.

### B1. Атомарный undo массового фикса — РЕШЕНО ✅

**Обещание паспорта:** «Все изменения одного bulk-действия — одна отменяемая транзакция».

**Проблема:** в Figma Plugin API нет транзакционного API. Группировка правок в один шаг истории — побочный эффект батчинга Figma, а **чанкинг обхода через `await setTimeout(0)` (который паспорт требует в блоке производительности) с высокой вероятностью разрывает батч на несколько шагов undo**. Два требования паспорта конфликтуют напрямую. Пользователь, применивший 200 фиксов и нажавший Cmd+Z, получил бы откат двенадцати — ровно тот сценарий, про который паспорт пишет «хоронит доверие навсегда».

**Решение принято 2026-08-23: собственный откат.** Подробности и отвергнутые варианты — [docs/decisions/0001-undo-strategy.md](decisions/0001-undo-strategy.md).

- [x] `FixAction` расширен обязательными `property` / `previousValue` / `nextValue`. Фикс, который не может назвать предыдущее значение, не применяется
- [x] `src/main/fixes/journal.ts` — `UndoJournal`: откат в обратном порядке, удалённая нода = пропуск, а не ошибка; резолвер ноды инжектится снаружи (совместимо с dynamic-page)
- [x] 5 тестов, включая порядок отката и частичный откат при удалённой ноде
- [ ] Кнопка «Отменить последнее массовое действие» в UI — при реализации `features/fix`
- [ ] **Текст в UI про Cmd+Z.** Нативная история остаётся неопределённой: Cmd+Z откатит какую-то часть. Без явной подсказки пользователь решит, что плагин сломан

### B2. Хэш ноды для инкрементального ре-скана — РЕШЕНО ✅

**Обещание паспорта:** `cache.ts` — «хэш ноды (`id + version-подобная сигнатура`)».

**Проблема:** у `SceneNode` нет публичного поля версии или времени модификации. «Version-подобная сигнатура» — это сериализация всех проверяемых свойств, которая стоит примерно столько же, сколько сам прогон правил.

**Решение принято 2026-08-23: инвалидация событиями.** Подробности — [решение 0003](decisions/0003-incremental-invalidation.md).

> **Поправка к рекомендации этого аудита.** Выше рекомендовался `figma.on('documentchange')`. Сверка с `@figma/plugin-typings` показала, что под dynamic-page этот путь требует `figma.loadAllPagesAsync()` — то есть отменяет ленивую загрузку, ради которой dynamic-page и выбран. Использован `PageNode.on('nodechange')`, как и советуют сами typings.

- [x] `src/main/scanner/cache.ts` — `IncrementalCache`: подписка на страницы скоупа, разметка грязного, `take()` с очисткой, `reset()` при смене скоупа, `dispose()` при закрытии
- [x] `CREATE` / `DELETE` помечают грязным и родителя — у контейнера меняется состав детей
- [x] 8 тестов, включая дедупликацию и снятие подписок
- [ ] **Проговорить в UI**, что кэш живёт только внутри сессии: первый скан после открытия плагина всегда полный
- Известное ограничение: `DELETE` приходит с `RemovedNode` без `parent`, поэтому бывший родитель удалённой ноды не помечается. Принято осознанно

### B3. Динамическая загрузка страниц и full-file скан — РЕШЕНО ✅

**Проблема:** манифест требует `documentAccess`. В режиме `"dynamic-page"` страницы не загружены по умолчанию; `figma.root.findAll()` требует `loadAllPagesAsync()`, `page.findAll()` — `page.loadAsync()`, а синхронный `getNodeById` **бросает исключение**. Описание `indexer.ts` в паспорте как «один `findAll()`» для full-file скана неверно.

Всё перечисленное проверено по `@figma/plugin-typings` из `package-lock.json`, а не по памяти.

**Решение принято 2026-08-23.** Подробности — [решение 0002](decisions/0002-dynamic-page-traversal.md).

- [x] `src/main/scanner/traversal.ts` — `ScanTarget` как дискриминированный юнион `selection` / `page` / `file`
- [x] **Ленивая постраничная загрузка** вместо `loadAllPagesAsync`: отмена на третьей странице из сорока не оплачена загрузкой сорока
- [x] Обход итеративный, на явном стеке — глубина вложенности не ограничена стеком вызовов JS
- [x] Два уровня прогресса (страницы + ноды), отмена на границе чанка и страницы, частичный результат вместо исключения
- [x] `yieldToEventLoop` инжектируется — иначе чанкинг непроверяем в тестах
- [x] Синхронные API уже запрещены линтером (блок C2)
- [x] 10 тестов, включая «после отмены оставшиеся страницы не грузятся»
- **Дефолтный скоуп «страница» получил архитектурное обоснование:** он не грузит остальные страницы, то есть дешевле качественно, а не только быстрее
- [ ] Открытый вопрос: правила, которым нужен весь файл (`orphan-variable`, `near-duplicate-component`), должны уметь объявлять это и отключаться вне скоупа `file`. Механизм — при реализации реестра правил

### B4. Библиотечные переменные — РЕШЕНО ✅

Паспорт риск называл, но решения не давал.

**Решение принято 2026-08-23: три состояния в типе + двухфазный скан.** Подробности — [решение 0004](decisions/0004-variable-resolver.md).

- [x] `src/main/scanner/variables.ts` — `VariableResolver` с состояниями `local` / `library` / `unavailable`
- [x] `resolve(id)` **синхронный** — это то, что позволяет `check` оставаться чистой функцией
- [x] `canJudge(id)` — правило спрашивает разрешение на вердикт, а не проверяет `null` по своему усмотрению. Дефолт для `unavailable` — молчать
- [x] Упавший запрос трактуется как `unavailable`, а не роняет скан
- [x] `areLayersConfigured` — без конфига `tokenLayers` правило `layer-violation` отключается, а не гадает
- [x] Матчинг коллекции со слоем — по точному имени, не по подстроке: `Semantic` не должен матчить `Semantic Legacy`
- [x] 11 тестов, включая ошибку API и подстрочный матч

**Найдено при реализации: скан обязан быть двухфазным.** `getLocalVariablesAsync` отдаёт только локальные переменные; библиотечная достаётся лишь `getVariableByIdAsync(id)` — по одному id и асинхронно, а список id известен только **после** обхода дерева. Формулировку паспорта «индекс переменных строится до обхода дерева» надо читать как «локальный индекс — до обхода, библиотечный достраивается после». Порядок: локальный индекс → обход со сбором id → гидрация → синхронный прогон правил.

### B5. `orphan-variable` даёт ложные срабатывания by design

**Проблема:** переменная, не используемая в текущем файле, может активно использоваться в файлах-потребителях библиотеки. Плагин видит один файл. Пометить такую переменную как сироту — прямая дезинформация, ведущая к удалению живого токена.

**Что сделать:**

- [ ] Ограничить правило файлами, не являющимися источником библиотеки, либо
- [ ] Понизить до `info` с формулировкой, которая честно говорит об ограничении области видимости («не используется **в этом файле**; проверьте потребителей библиотеки вручную»), и
- [ ] **Не давать `fix`** этому правилу ни при каком раскладе.

### B6. `a11y/token-pair-contrast` — комбинаторика

Проверка «пар токенов» без ограничения — это N×M пар, из которых осмысленны единицы. Отчёт на 4000 пар бесполезен.

**Что сделать:**

- [ ] Определить источник пар: реально встречающиеся в файле сочетания `fill` ноды + `fill` её текстового потомка, а не декартово произведение коллекции.
- [ ] Учесть режимы (light/dark): пара валидна во всех режимах или ни в одном. Пересечение с правилом `missing-mode-value`.

### B7. Рост снапшотов против лимита plugin data

Релиз 3 пишет снапшоты в `documentPluginData`. Хранилище plugin data ограничено по объёму (паспорт называет ~100 KB на запись для нод). Тренд, копящийся неделями, упрётся в лимит и начнёт молча терять данные.

**Что сделать:**

- [ ] Заложить политику ротации сразу при проектировании формата снапшота: N последних + агрегаты, а не полная история.
- [ ] Формат снапшота — версионированный (`{ v: 1, ... }`), иначе миграция тренда при изменении набора метрик потеряет историю.

### B8. Конфиг в документе — РЕШЕНО ✅

`figma.root.setPluginData('audit-config')` при командной работе — last-write-wins без уведомления. Два DS-мейнтейнера переписывают конфиг друг друга молча.

- [x] `src/main/storage/config.ts` — поля `updatedBy` + `updatedAt`, `stampAuthorship()`. Полноценного мерджа нет, есть видимость авторства
- [x] Разбор недоверенного JSON **не бросает исключений**: вход приходит и из `pluginData` чужой версии плагина, и из импортированного руками файла. Возвращается рабочий конфиг плюс список замечаний
- [x] **Неизвестные id правил сохраняются, а не выбрасываются.** Конфиг общий на команду, у коллеги может стоять версия новее — выбросить незнакомое правило значит молча стереть чужую настройку
- [x] Превышение лимита plugin data — исключение, а не тихое обрезание. Длина считается в байтах UTF-8 вручную: `TextEncoder` в plugin sandbox не гарантирован
- [x] 15 тестов
- [ ] Показать замечания разбора в UI настроек — иначе правки конфига исчезают молча

---

## Блок C. Инфраструктура репозитория — ВЫПОЛНЕНО

Каркас собран 2026-08-23. `npm run verify` (typecheck + lint + test + build + бюджет) проходит зелёным.

Ключевая идея реализации: соглашения паспорта переведены из разряда договорённостей в разряд падающих проверок. Границу `main` / `ui` держат **раздельные `tsconfig`** (в `tsconfig.main.json` нет `lib: DOM`, в `tsconfig.ui.json` нет `@figma/plugin-typings`) плюс правила ESLint. Нарушение не компилируется и не линтуется.

### C1. Каркас сборки ✅

- [x] `package.json`, TS strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` в `tsconfig.base.json`
- [x] `@figma/plugin-typings` — только в `tsconfig.main.json` и `tsconfig.test.json`
- [x] `manifest.json`: `editorType`, `documentAccess: "dynamic-page"`, `networkAccess: { allowedDomains: ["none"] }`
- [x] esbuild → `dist/code.js` (IIFE, es2017, `platform: neutral`) — `scripts/build-main.mjs`
- [x] Vite + `vite-plugin-singlefile` → `dist/index.html`
- [x] `scripts/size-check.mjs` — сборка падает при выходе за 500 KB
- **Текущий расход бюджета: 145.6 KB из 500 KB.** Остаток на Shadcn UI, дашборд и превью-дифф — 354 KB

### C2. Качество кода ✅

- [x] ESLint 10 flat config + `recommendedTypeChecked`, `no-explicit-any: error`
- [x] Границы импортов: React и `*/ui/*` запрещены в `src/main/**`; `*/main/*` запрещён в `src/ui/**`
- [x] `document` / `window` запрещены в `src/main/**`; `figma` запрещён в `src/ui/**`
- [x] Запрет 10 синхронных Figma-API (`getNodeById`, `getLocalVariables`, `getVariableById`, …) в `src/main/**` через `no-restricted-syntax` — прямая страховка от B3
- [x] Prettier + `format:check` в CI
- [ ] lint-staged + pre-commit hook — **не сделано**, сознательно: CI закрывает тот же класс ошибок, а хук раздражает при частых WIP-коммитах. Добавить, если начнут прилетать красные PR

Срабатывание всех четырёх правил границ проверено пробным файлом-нарушителем, не осталось в репозитории.

### C3. Тесты ✅

- [x] Vitest 4 + `test/factories/node.ts` — фабрика моков: `frame`, `text`, `rectangle`, `component`, `instance`, `solidPaint`, `variableAlias`, `tree` (двусторонняя связка parent/children), `resetIds`
- [x] `test/factories/idempotency.ts` — `expectIdempotent(target, applyFix, snapshot)` для DoD п.4
- [x] Порог покрытия `src/main/rules/**`: lines/functions/statements 90%, branches 80%
- [x] 7 тестов на саму фабрику и хелпер — включая негативный кейс, где `expectIdempotent` обязан упасть

### C4. CI ✅

- [x] `.github/workflows/ci.yml`: typecheck → lint → format:check → test → build + бюджет размера, на PR и push в `main`
- [ ] **Branch protection на `main`** — единственное, что нельзя сделать из репозитория: настраивается в Settings → Branches. Запретить прямой push, требовать зелёный `verify`
- ~~Токен `gh` без scope `workflow`~~ — **неактуально.** Remote настроен на SSH, а ограничение scope действует только на push по HTTPS с OAuth-токеном. `gh auth refresh` не нужен

### C5. Процесс ✅

- [x] `.github/pull_request_template.md` — DoD правила прямо в шаблоне, включая поле для цифры сигнала
- [x] `CONTRIBUTING.md` — рецепт добавления правила, таблица границ, пример хорошей и плохой формулировки сообщения, отсылка к открытым решениям B1–B4

---

## Блок D. Публикация в Figma Community ⚠️ БЛОКЕР релиза

Не техническая работа, но срывает дату, если начать за неделю до релиза. Требования проверить по актуальной доке Figma перед подачей — они меняются.

- [x] **Лицензия репозитория — MIT.** Плагин открыт полностью. Форк и переиздание в Community юридически возможны — защита строится Adoption-данными и скоростью, а не лицензионным запретом
- [~] **Политика приватности — черновик готов: [PRIVACY.md](../PRIVACY.md).** Написан разработчиком, не юристом; **требует проверки перед подачей плагина**. Описывает фактическое поведение кода: что читается, что пишется в `pluginData`, почему сетевых запросов нет. Сверять при каждом изменении `networkAccess` в манифесте
- [ ] Аккаунт паблишера, обложка, иконка, описание, скриншоты, теги
- [ ] Заложить **буфер на ревью Figma** — сроки не контролируются командой
- [ ] Канал поддержки и адрес для баг-репортов
- [ ] Политика версионирования и changelog

---

## Блок E. Продуктовые решения, влияющие на код

- [ ] **Формулировки сообщений.** DoD требует «что не так и почему». Написать текст **до** реализации правила: правило, для которого не формулируется внятное «почему», — кандидат на выброс. Это дешёвый фильтр каталога
- [~] **Пресеты.** Формат конфига зафиксирован и разбирает `extends` как произвольную строку, так что миграции 1 → 2 из-за пресетов не будет. Но **содержимое пресетов определить сейчас нельзя**: `recommended` — это список правил с уровнями, а какие правила выживут, известно только после блока A. Наполнять после заполнения [results.md](validation/results.md)
- [ ] **Поведение при отсутствующем `tokenLayers`.** Паспорт: правило отключается, а не гадает. Нужен видимый UI-сигнал «правило отключено, потому что не настроено» — иначе пользователь считает, что нарушений нет
- [ ] **Пустое состояние.** Первый запуск на чистом файле не должен выглядеть как «плагин не работает»
- [ ] **Формат экспорта отчёта** (релиз 3) — определяет, нужен ли `networkAccess` вообще

---

## Сводка: блокеры и порядок

| #   | Работа                                  | Блок | Статус                                                                                                   |
| --- | --------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------- |
| 1   | Валидация сигнала на 3 боевых файлах    | A    | **ОТКРЫТ.** Процедура написана ([протокол](validation/protocol.md)), нужны боевые файлы и дизайнер-судья |
| 2   | Стратегия undo массового фикса          | B1   | Снято 2026-08-23 — [решение 0001](decisions/0001-undo-strategy.md)                                       |
| 3   | Замена «хэша ноды» на реальный механизм | B2   | Снято 2026-08-23 — [решение 0003](decisions/0003-incremental-invalidation.md)                            |
| 4   | Модель dynamic-page в traversal         | B3   | Снято 2026-08-23 — [решение 0002](decisions/0002-dynamic-page-traversal.md)                              |
| 5   | `VariableResolver` с тремя состояниями  | B4   | Снято 2026-08-23 — [решение 0004](decisions/0004-variable-resolver.md)                                   |
| 6   | Лицензия                                | D    | Снято 2026-08-23 — MIT                                                                                   |
| 7   | Политика приватности                    | D    | Черновик готов ([PRIVACY.md](../PRIVACY.md)); **нужна ваша проверка** перед подачей                      |

Пункты 2–5 были **расхождениями паспорта с API**, а не только пробелами в коде. Решения приняты, реализованы и **внесены в `CLAUDE.md`**: «хэш ноды» заменён на инвалидацию событиями, «один `findAll()`» — на ленивую постраничную загрузку, «транзакция» — на собственный откат, добавлен раздел о двухфазности скана и пункт 6 в DoD правила. Паспорт и код больше не расходятся.

## Что сознательно не делаем сейчас

- Правила блоков `structure/`, `content/`, `a11y/` — релиз 4 по паспорту, трогать до MVP нет причин
- Impact analysis и снапшоты — релизы 2 и 3; но **формат снапшота** (B7) стоит зафиксировать заранее, потому что от него зависит совместимость истории
- Оптимизация под 150k нод до того, как есть что оптимизировать и на чём мерить
